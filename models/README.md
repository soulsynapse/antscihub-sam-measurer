# Model Weights

This folder is where the SAM ONNX encoder and decoder weights are downloaded.

The model files are large generated assets and are intentionally not tracked in
Git. The app downloads missing weights automatically when a model is selected.
